#include <stdio.h>

int main()
{
	printf("short: %ld, int: %ld, long: %ld, float: %ld, double: %ld\n",		    sizeof(short), 
		sizeof(int), 
		sizeof(long), 
		sizeof(float), 
		sizeof(double));

	return 0;
}
